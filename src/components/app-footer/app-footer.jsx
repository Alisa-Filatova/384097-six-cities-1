import React from 'react';
import AppLogo from '../app-logo/app-logo.jsx';

const AppFooter = () => (
  <footer className="footer container">
    <AppLogo prefix="footer" small />
  </footer>
);

export default AppFooter;
